import React from 'react';
import { Switch, Route } from 'react-router-dom';

import { ROUTES, ROUTE_SUFFIX } from '../../constants';

const CrmModule = () => {
	const routesApp = ROUTES.crm;

	return <Switch></Switch>;
};

export default CrmModule;
