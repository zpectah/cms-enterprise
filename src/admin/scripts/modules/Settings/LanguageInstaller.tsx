import React from 'react';

interface LanguageInstallerProps {}

const LanguageInstaller = ({}: LanguageInstallerProps) => {
	return (
		<>
			<div>...LanguageInstaller...</div>
		</>
	);
};

export default LanguageInstaller;
